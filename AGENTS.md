# X2RED Agent Context

本文件是所有自动化编码 Agent、ChatGPT 对话和新贡献者进入 X2RED 时的第一读取入口。

## 必读顺序

1. `docs/ai-context/README.md`
2. `docs/ai-context/PROJECT_MEMORY.md`
3. 与当前任务相关的 `docs/ai-context/WORKFLOWS.md` 或 `DEVELOPMENT_PLAYBOOK.md`
4. `docs/ai-context/OPEN_ITEMS.md`
5. 再读取实际代码、PR 和 CI；不要只凭本文假设仓库现状。

## 当前产品定义

X2RED 是本地优先的中文内容研究、语料组织、多工作台创作、视觉制图、审核和人工发布系统。当前产品必须收拢为三层主架构：

1. **语料素材库**：X 信号发现、简中平台发现、平台分类来源、语料池和批次。
2. **内容工作台**：小红书、公众号长文、公众号轻内容、审核与发布。
3. **模型与 Skill**：文本模型、风格配置、Guizang、Minimal Zine 等原生 Skill。

核心链路：

`X / 简中平台发现 → 标准来源 → 平台分类 → 语料池 → 冻结批次 → 指定工作台 → 编辑 / 制图 / 审核 / 发布`

## 非协商约束

- 不要把所有来源重新塞回一个无分类的超长下拉框。
- 信号台负责 X 的发现；原料库负责简中平台发现；两者最终都进入统一 `SourceItem` 体系。
- 语料池是可复用内容资产，不是一次性生成表单。
- 语料池批次必须冻结当时的池版本、全池记忆、详细来源和 provenance。
- `corpus_batch` 关系保持单向、单层隔离，避免旧批次经共享来源污染后续上下文。
- 公众号轻内容生成图组前，必须使用当前候选和当前编辑框内容，不能仅提交旧版本 ID。
- 图片模型不得负责最终中文排版。Minimal Zine 中，图片模型只生成无字视觉锚点，中文和最终版式由 X2RED 本地合成。
- 所有最终 Minimal Zine 文件必须位于 `data/exports/wechat/{variant_id}/`，并同步重建预览和发布包。
- 不得声称图片模型绝对不会产生水印或角标；当前策略是严格负面 Prompt、裁切高风险外沿和在本地干净画布上重新合成。
- MediaCrawler 仅允许本地、低频、研究和非商业学习场景，不复制或修改其上游源码，不绕过平台访问控制。
- 所有发布都需要人工事实和版权复核；X2RED 不点击最终发布按钮。
- 不要把 PR 是否已合并、CI 是否仍通过等易变化事实当作永久记忆；开始工作前必须重新查询 GitHub。

## 当前开发分支

文档创建时的功能分支为：

`agent/replace-crawlers-with-api-adapters`

分支名称保留了早期方案痕迹，但当前实现已经转向 MediaCrawler 负责简中平台发现，而不是继续以通用搜索 API 适配器作为主链路。

当前 PR 是 `troicc/X2RED#19`，文档创建前的代码头为 `4c3cf831bca53bfec27347cdd8f8290911dd4e2e`。开始任何后续工作前，请重新检查 PR 状态、当前 head 和 CI。

## 修改后的记忆维护规则

完成以下任一事项时，必须同步更新 `docs/ai-context/`：

- 主架构、数据模型或核心流程变化；
- 新增或移除平台、工作台、模型或 Skill；
- PR 合并、分支切换或版本发布；
- 重要 bug 的根因和修复方式变化；
- CI、数据库迁移、启动方式或环境变量变化；
- 用户确认新的产品原则、优先级或禁止事项。

更新时至少修改：

- `PROJECT_MEMORY.md` 的状态和决策；
- `OPEN_ITEMS.md` 的完成项、风险和下一步；
- `context.json` 的机器可读摘要和更新时间。

不要把访问令牌、Cookie、API Key、用户隐私数据或本机绝对路径写入这些文档。